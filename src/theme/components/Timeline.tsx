import { Theme } from '@mui/material';
import { Components } from '@mui/material/styles';

export const TimelineItem: Components<Omit<Theme, 'components'>>['MuiTimelineItem'] = {
  styleOverrides: {},
};
